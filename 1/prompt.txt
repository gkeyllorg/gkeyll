In this branch we've added a Jenkins-based CI system. Presently it only runs on single CPUs, or in the case of perlmutter_gpu it runs on a single GPU. I'd like to create a list of tests that will also run in parallel (on multiple GPUs on perlmutter_gpu or on multiple CPUs elsewhere). How would you recommend we implement this? perhaps we should restrict ourselves to first extend runregression so it can run tests with a given MPI/NCCL decomposition.
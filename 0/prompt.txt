Merge branch agent_tools into this branch. agent_tools introduced an auto-formatter with clang-format. This touched lines of code which were modified in this branch. Resolve the merge conflicts and preserve the changes from this branch. Use the merging-formatted-branches skill and prepare_merge.py

---

Merge branch agent_tools into this branch. agent_tools introduced an auto-formatter with clang-format. This touched lines of code which were modified in this branch. Resolve the merge conflicts and preserve the changes from this branch. Use the merging-formatted-branches skill and prepare_merge.py
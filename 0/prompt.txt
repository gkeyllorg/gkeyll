/home/maxwell-rosen/gkeyll/gyrokinetic/creg/rt_gk_mirror_boltz_elc_poa_1x2v_p1.c  in a POA simulation like this, I'd like to use a time dilation mask to soften the timestep by a factor 1/10 but ONLY IN THE COLLISIONLESS UPDATE. The current time dilation formulation from GKYL_GK_FDOT_MULTIPLIER_FIXED_FACTOR_TIMES_OMEGA_MAX uses total fdot, but I only want to modify the collisionless update in this implementation. Implement this screening of the collisionless term such that the timestep is modified in the same way that the fdot multiplier terms are used. An elegant implementation would re-use the infrastructure in the fdot multiplier, but in the collisionless parts, for instance /home/maxwell-rosen/gkeyll/gyrokinetic/apps/gk_species_collisionless.c . Implement and test this fully. An example for a production simulation which I'd like this to mirror is /home/maxwell-rosen/gkeyll/prod_ex.txt . During the OAP, I'd like the loss cone mask to be applied and also time dilation of the collisionless term.

---

/home/maxwell-rosen/gkeyll/gyrokinetic/creg/rt_gk_mirror_boltz_elc_poa_1x2v_p1.c  in a POA simulation like this, I'd like to use a time dilation mask to soften the timestep by a factor 1/10 but ONLY IN THE COLLISIONLESS UPDATE. The current time dilation formulation from GKYL_GK_FDOT_MULTIPLIER_FIXED_FACTOR_TIMES_OMEGA_MAX uses total fdot, but I only want to modify the collisionless update in this implementation. Implement this screening of the collisionless term such that the timestep is modified in the same way that the fdot multiplier terms are used. An elegant implementation would re-use the infrastructure in the fdot multiplier, but in the collisionless parts, for instance /home/maxwell-rosen/gkeyll/gyrokinetic/apps/gk_species_collisionless.c . Implement and test this fully. An example for a production simulation which I'd like this to mirror is /home/maxwell-rosen/gkeyll/prod_ex.txt . During the OAP, I'd like the loss cone mask to be applied and also time dilation of the collisionless term.